  Component // syntax, structure follows source
      |
      v
 Graph<Node> // semantics, structure follows semantic dataflow
      |
      v
 Graph<Stage> // buffers, structure follows actual dataflow
      |
      v

when combining stages into kernels, stages place compute+init into their own
unstacked loops before anything. then fusable axes are pruned. then
recursively, producer loops are stacked and injected into the corresponding
bodies of the (still unstacked) consumer stage.

