[project]
name = "ilang-python"
version = "0.1.0"
description = "Python front-end for 𝚒"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ilang"]

[tool.ruff]
target-version = "py310"

[tool.ty]
